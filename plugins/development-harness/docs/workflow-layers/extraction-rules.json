{"1":["Find every Mermaid flowchart diamond node: Q{...} or D{...} syntax. Extract the verbatim label text inside the braces as the decision_question. Record the exact source heading (## or ### header) the flowchart block appears under. Record the skill name from the file path."],"2":["Find every outgoing labeled edge from a Mermaid diamond node: syntax is NodeId -->|condition text| TargetId. Extract the condition text verbatim as the branch condition. Record which diamond (decision_question) this edge leaves from. Record the target node label verbatim."],"3":["Find branches where the target node label names a file path or skill: patterns include 'Load X.md', 'references/workflows/Y', '/skill-name', 'skills/Z/SKILL.md'. These are file-reference expansion links. Extract the file path or skill name as the target_file. Record the parent diamond and branch condition."],"4":["Find agent dispatch patterns: Agent(subagent_type=...), TeamCreate(...), subagent_type= string assignments, 'dispatches to', 'spawn', wave definitions in swarm files. Extract the agent name or subagent_type value verbatim. Record the dispatch mechanism (Agent vs TeamCreate vs other). Record the source heading."],"5":["Find MCP tool call patterns: mcp__plugin_dh_backlog__*, mcp__plugin_dh_sam__*, and named tool calls like backlog_groom(), backlog_view(), artifact_register(), artifact_read(), sam_plan(), sam_task(). Extract the tool name verbatim and any parameters shown. Record the source heading."],"6":["Find artifact flow patterns: artifact_register(...), artifact_read(...), artifact_list(...), backlog_groom(section=...), sam_plan(action='create',...). Extract the artifact_type or section name verbatim. Note whether this is a produce (register/create/write) or consume (read/list) operation. Record the source heading."]}