The purpose and explicit goals of the skill woo-sailor:

1. Apply Process Siren consistently across one file or a directory of eligible AI-facing process documents without requiring the user to invoke each file independently.
2. Route bulk work into explicit analyze, improve, or represent behavior while preserving read-only operation when requested.
3. Isolate per-file processing so one blocked or defective process does not prevent useful work on unrelated eligible files.
4. Synthesize process models across file boundaries to expose caller/callee contradictions, conflicting assumptions or invariants, and unclear ownership or recovery behavior.
5. Return a coherent aggregate result that preserves per-file evidence, blockers, changes, validation status, and cross-file findings.
