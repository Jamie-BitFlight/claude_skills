The purpose and explicit goals of the skill woo-sailor:

1. Apply Process Siren consistently across one file or a directory of eligible AI-facing process documents without requiring the user to invoke each file independently.
2. Route bulk work into explicit ANALYZE, IMPROVE, or REPRESENT behavior while preserving read-only operation when requested.
3. Isolate per-file processing so blocked or invalid processes do not prevent useful work on unrelated eligible files, while preserving each file's evidence and status.
4. Synthesize semantic process models across material file boundaries to expose caller/callee contract conflicts, incompatible assumptions or invariants, and ownership/recovery gaps that local analysis cannot reveal.
5. Revalidate claims and interfaces affected by cross-file findings or changes before finalizing bulk improvement, and return a coherent aggregate result without treating per-file success as proof of system coherence.
