# Python Development Orchestration

The canonical workflow authority is the `python-engineering:orchestrating-python-development`
skill. Do not maintain a second copy of its workflow diagrams or handoff contracts here.

Load that skill when selecting a multi-step Python workflow, agent chain, review loop, or
implementation/test ordering. `python3-core` owns routing defaults; the orchestration skill owns
sequencing and completion contracts.
