backend:
  name: github

task:
  backend: local

context:
  backend: local

sam:
  plan_index_issue: 2531  # SAM Plan Registry sentinel — do not close or rename

gh:
  repo: Jamie-BitFlight/claude_skills
