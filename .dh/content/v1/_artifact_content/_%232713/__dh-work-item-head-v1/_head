{"version":1,"reference":{"kind":"artifact_content","namespace":"#2713","artifact_type":"_dh-work-item-head-v1","name":"head"},"owner_reference":"#2713","content":"{\"version\":1,\"issue_reference\":\"#2713\",\"parent_revision\":\"2d923ec5dec61057edf39833770513eab9080d17\",\"root_revision\":\"8a1e619b02f3ccd3e764b94643822d7ae815602bc2a6bf51aa2f67cc096e8e12\",\"body\":\"<!-- backlog-metadata:\\npriority: P1\\ntype: Bug\\nstatus: open\\nadded: 2026-08-12\\n-->\\n\\n## Description\\n\\nTwo `normalize_location()` behavior changes were required to ship behind a backward-compatibility gate (a flag or version check) so existing callers keep the old behavior \u2014 this requirement is not met. The new heading-normalization behavior applies unconditionally to every caller.\\n\\nWhere observed: PR #2626 code review, `plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py` \u2014 a shared cross-plugin file, not owned by DH.\\n\\nImpact: any existing or future consumer of `reduce.py`'s `normalize_location()` gets the new corroboration-keying behavior with no way to opt out, risking silent behavior changes for tools that depend on this shared utility.\\n\\n## Fact-Check\\n\\n<div><sub>2026-07-06T05:42:14.775368Z</sub>\\n\\nCLAIM 1: \\\"Two normalize_location() behavior changes were required to ship behind a backward-compatibility gate\\\"\\nVERDICT: INCONCLUSIVE\\n\\nEVIDENCE:\\n  Source: /home/user/claude_skills/plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py lines 81-97\\n  Retrieved: 2026-07-06\\n  Content: |\\n    def normalize_location(raw: str) -> str:\\n        \\\"\\\"\\\"Normalize a location to `path:line`, preserving the path to avoid false merges.\\n        ...\\n        \\\"\\\"\\\"\\n        match = LOCATION_LINE_RE.search(raw)\\n        if match:\\n            path = match.group(1).lstrip(\\\"/\\\")\\n            return f\\\"{path}:{match.group(2)}\\\"\\n        return raw.strip()\\n\\nCROSS_CHECK:\\n  Source: Git history \u2014 file created in commit 4a570e2 (2026-06-07), no prior versions\\n  Finding: File is a new addition, not a modification. No evidence of multiple behavior-change versions. Only one implementation of normalize_location() exists in the codebase.\\n\\nEXPLANATION: The claim references \\\"two behavior changes\\\" but does not explicitly name them. The normalize_location() function implements a single behavior (extract path:line and normalize path). Without explicit identification of which two changes the claim refers to, and no evidence of multiple behavior versions in git history, the claim status is inconclusive.\\n\\nCITATION: |\\n  SOURCE: /home/user/claude_skills/plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py (accessed 2026-07-06)\\n  VERIFIED_BY: Read (file content inspection), Bash (git history)\\n\\n---\\n\\nCLAIM 2: \\\"The new heading-normalization behavior applies unconditionally to every caller\\\"\\nVERDICT: REFUTED\\n\\nEVIDENCE:\\n  Source: /home/user/claude_skills/plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py line 46\\n  Retrieved: 2026-07-06\\n  Content: |\\n    LOCATION_LINE_RE = re.compile(r\\\"(\\\\S+):(\\\\d+)\\\")\\n\\nCROSS_CHECK:\\n  Source: Functional test of LOCATION_LINE_RE against markdown paths with anchors\\n  Finding: Regex test against \\\"file.md#heading:10\\\" \u2192 matches as path=\\\"file.md#heading\\\" (anchor preserved, NOT stripped). Test against \\\"path.md#anchor\\\" (no line) \u2192 no match (anchor preserved in fallback).\\n\\nEXPLANATION: The normalize_location() function does not strip heading anchors (`#anchor` patterns). The regex captures non-whitespace before `:digit`, preserving any `#` characters in the path. No heading-normalization behavior exists in the current code.\\n\\nCITATION: |\\n  SOURCE: /home/user/claude_skills/plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py line 46 (accessed 2026-07-06)\\n  VERIFIED_BY: Bash (regex functional test)\\n\\n---\\n\\nCLAIM 3: \\\"Any existing or future consumer of reduce.py's normalize_location() gets the new corroboration-keying behavior with no way to opt out\\\"\\nVERDICT: VERIFIED (with qualification)\\n\\nEVIDENCE:\\n  Source: /home/user/claude_skills/plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py line 81\\n  Retrieved: 2026-07-06\\n  Content: |\\n    def normalize_location(raw: str) -> str:\\n\\n  Function has no parameters, flags, or version checks to control behavior.\\n\\nCROSS_CHECK:\\n  Source: Function usage in reduce_findings() lines 148-149\\n  Finding: normalize_location() is called unconditionally on every location; result is used unconditionally as dedup key component.\\n\\nEXPLANATION: The function signature has no opt-out parameters. However, \\\"corroboration-keying behavior\\\" is not a feature of normalize_location() itself \u2014 it is how the OUTPUT is used (as dedup key in line 149). The function is unconditionally applied with no variant or flag, confirming the claim's assertion about lack of opt-out.\\n\\nCITATION: |\\n  SOURCE: /home/user/claude_skills/plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py lines 81, 148-149 (accessed 2026-07-06)\\n  VERIFIED_BY: Read (function signature and usage inspection)\\n\\n---\\n\\nCLAIM 4: \\\"reduce.py is a shared cross-plugin file, not owned by DH\\\"\\nVERDICT: REFUTED\\n\\nEVIDENCE:\\n  Source: File system path: /home/user/claude_skills/plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py\\n  Retrieved: 2026-07-06\\n  Content: Directory structure shows file is located at plugins/plugin-creator (plugin name), not in a shared utilities location. The file resides in the ensemble-rule-review skill's scripts/ directory.\\n\\nEXPLANATION: The file is owned by the plugin-creator plugin (skill: ensemble-rule-review), not by a shared utility location or the Development Harness (DH) plugin. The claim that it is \\\"not owned by DH\\\" is technically correct, but the file is also not \\\"shared cross-plugin\\\" \u2014 it belongs to a specific plugin-creator skill.\\n\\nCITATION: |\\n  SOURCE: Directory structure at /home/user/claude_skills/plugins/plugin-creator/skills/ensemble-rule-review/scripts/ (accessed 2026-07-06)\\n  VERIFIED_BY: Glob pattern match and Read of repository structure\\n</div>\\n\\n## RT-ICA\\n\\n<div><sub>2026-07-06T05:48:49.711687Z</sub>\\n\\nRT-ICA Snapshot: fix: reduce.py's normalize_location changes shipped without the required backward-compat gate\\nDate: 2026-07-06\\nGoal: Ensure a backward-compatibility gate exists before any future behavior changes to normalize_location() can land (from PR #2626 or subsequent PRs), so existing callers are not silently broken.\\nConditions:\\n1. normalize_location() current behavior in main branch | Status: AVAILABLE \u2014 strips absolute path prefix, normalizes to path:line via LOCATION_LINE_RE = r\\\"(\\\\S+):(\\\\d+)\\\"; heading anchors preserved (REFUTED by fact-checker)\\n2. Heading-normalization behavior present in current code | Status: AVAILABLE \u2014 REFUTED by fact-checker; regex test confirms anchors are NOT stripped; this is a FORWARD GATE REQUIREMENT, not a remedial fix\\n3. Gate mechanism design choice | Status: AVAILABLE \u2014 stated in issue description: \\\"a flag or version check\\\"; Python convention and codebase context derive the answer: add a keyword parameter to normalize_location() with backward-compatible default; no human decision required\\n4. Whether any backward-compat gate currently exists | Status: AVAILABLE \u2014 VERIFIED: normalize_location() has no parameters; zero opt-out surface\\n5. Current callers of normalize_location() outside reduce.py | Status: AVAILABLE \u2014 confirmed by impact-analyst: scorer.py at examples/solid-review-ab/runner/scorer.py imports via importlib; corpus/gold.json is downstream target; tight coupling with no error on metric corruption\\n6. test_reduce.py CI coverage gap | Status: AVAILABLE \u2014 confirmed by impact-analyst: test_reduce.py is NOT in pyproject.toml testpaths; test DERIVABLE fix: add path to testpaths; this is a concrete deterministic change, not a design decision\\n7. Whether three test files test the current normalize_location contract | Status: AVAILABLE \u2014 confirmed: test_reduce.py, test_scorer.py, test_scorer_independent.py all test current behavior; none test heading-anchor inputs or gate semantics\\n8. Proposed changes from PR #2626 that triggered this item | Status: DERIVABLE \u2014 PR #2626 is open (pr-2626 branch); reading that branch's reduce.py would confirm what heading-normalization changes are proposed\\nAVAILABLE count: 7\\nDERIVABLE count: 1\\nMISSING count: 0\\nOverall verdict: READY \u2014 all blocking conditions resolved via derivation and fact-check results\\n</div>\\n\\n## Impact Radius\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\n> **Critical pre-analysis finding**: The \\\"heading-normalization\\\" behavior described in this\\n> issue is **NOT present in the current codebase**. `normalize_location()` in `reduce.py` contains\\n> only path-prefix stripping and `path:line` extraction via `LOCATION_LINE_RE`. The issue was\\n> generated from a code review of PR #2626, meaning the proposed behavior changes exist in a PR\\n> branch, not on main. This item's requirement is: **any PR that adds heading normalization to\\n> `normalize_location()` must simultaneously add a backward-compat gate (a parameter with a\\n> default that preserves the current behavior)**. The impact analysis below describes what would\\n> be affected if those changes ship without the gate.\\n\\n### Code - Producers\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py::normalize_location` \u2014 owns the location-normalization contract; currently takes no parameters, making it impossible for callers to opt out of any behavior change; any addition of heading-normalization behavior here propagates unconditionally to every consumer | Risk: HIGH | Why: zero-parameter API with no opt-out surface; single point of failure for all downstream corroboration keying; change without gate silently corrupts keys in scorer, gold-label matching, and dedup\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py::reduce_findings` \u2014 calls `normalize_location(finding.location)` internally to build corroboration keys; adding heading-normalization changes every `(group, location)` key where a heading anchor was present, potentially collapsing distinct findings or separating corroborated ones | Risk: HIGH | Why: dedup key generation changes silently; no test currently exercises heading-anchor inputs\\n\\n### Code - Consumers\\n\\n- `examples/solid-review-ab/runner/scorer.py::normalize_location` \u2014 imports `reduce.py` via `importlib` at module load time; wraps `_reduce.normalize_location(raw)` and appends `../`-stripping; any behavior change to reduce.py's function propagates directly into the scorer's key computation for P/R/F1 matching against gold labels | Risk: HIGH | Why: hard-coupled via `importlib`; no abstraction layer; behavior change would silently misalign (group, location) keys against `corpus/gold.json`, corrupting all metrics; `_REDUCE_PATH` is the declared contract \u2014 \\\"the import is the contract\\\"\\n\\n- `examples/solid-review-ab/runner/scorer.py::reduce_findings` \u2014 delegates to `_reduce.reduce_findings(reports, keep_threshold)` directly; inherits any behavior change to corroboration keying | Risk: MEDIUM | Why: secondary consumer surface; affected by same key-change as `normalize_location`, but deduplication of findings is less sensitive to heading-anchor edge cases than exact-key matching against gold\\n\\n- `examples/solid-review-ab/runner/scorer.py::reported_keys` \u2014 calls `normalize_location(f.location)` to build the reported set for metrics; gold label set uses `path:line` format without heading anchors; a mismatch in normalization output format between reported keys and gold keys directly zeros all P/R/F1 scores with no error surfaced | Risk: HIGH | Why: zero-overlap failure is silent \u2014 function returns empty TP set, full FN set; the built-in path-mismatch warning only fires when ALL keys miss, not when heading-normalized keys partially fail to match\\n\\n### Code - Other References\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/scripts/plan_ensemble.py` \u2014 pairs with `reduce.py` as the map-side bookend; does not call `normalize_location` directly but is functionally coupled to reduce.py's schema contract | Risk: LOW | Why: no direct API call to normalize_location; coupling is architectural, not behavioral\\n\\n- `examples/solid-review-ab/corpus/gold.json` \u2014 contains gold labels with `path:line` location format (e.g. `corpus/cases/01_srp_god_object.py:17`); normalize_location's output must match these keys exactly for TP/FP/FN computation; gold labels do not contain heading anchors, so adding heading normalization would be asymmetric unless gold is updated | Risk: HIGH | Why: gold labels are handcrafted fixtures; a silent behavior change makes them permanently incompatible with arm output keys that run through the new normalization; no runtime error is raised \u2014 metrics just silently degrade\\n\\n### Tests\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/scripts/test_reduce.py::test_normalize_location_strips_abs_prefix_but_keeps_path` \u2014 asserts current behavior: strips absolute prefix, preserves directory, returns `path:line`; no test for heading inputs (`path/file.py#heading`) or the compat gate parameter | Risk: MEDIUM | Why: test encodes old contract; would pass when compat gate is added (old behavior is default), but must be extended to cover new behavior and gate semantics; **this file is NOT in the main CI testpaths** \u2014 runs manually from the `scripts/` directory only, so behavior regressions would not be caught in automated CI\\n\\n- `examples/solid-review-ab/tests/test_scorer.py` \u2014 four tests explicitly verify the `normalize_location` contract via scorer wrapper: strip leading `/`, preserve directory, trim whitespace, no-line return; all encode current behavior only | Risk: MEDIUM | Why: tests would remain green under a properly-gated change (if scorer opts out), but would fail if scorer inherits new behavior without explicitly opting in or out; these tests ARE in the main CI testpaths\\n\\n- `examples/solid-review-ab/tests/test_scorer_independent.py` \u2014 three independently-authored tests for `normalize_location`: `test_normalize_location_absolute_path_equals_relative_path`, `test_normalize_location_same_basename_different_dirs_remain_distinct`, `test_normalize_location_whitespace_is_trimmed`; all encode current behavior only | Risk: MEDIUM | Why: independent test suite (authored from first principles, not derived from scorer output) would detect behavior drift if scorer inherits new normalization behavior without the tests being updated; these tests ARE in the main CI testpaths\\n\\n- **Coverage gap**: No test anywhere verifies (a) heading-anchor inputs through normalize_location, (b) the compat gate parameter when it is added, (c) old callers receiving old behavior by default after the gate is added | Risk: HIGH | Why: adding the gate without tests for the gate itself leaves the compatibility guarantee unverified\\n\\n### Documentation\\n\\n- `examples/solid-review-ab/README.md` \u2014 line 84: \\\"Normalization for matching MUST match `reduce.py`'s `normalize_location` (strip a leading `/`, trim whitespace, PRESERVE the directory)\\\"; this is a behavioral contract specification for arm authors and gold label authors; it describes only the current behavior, not heading normalization | Risk: MEDIUM | Why: arm authors building new arms follow this contract specification; if heading normalization ships without updating this file, new arm authors would produce gold labels and location strings inconsistent with the new behavior\\n\\n- `examples/solid-review-ab/.claude/agent-memory/python-engineering-python-pytest-architect/project_scorer_independence.md` \u2014 documents `normalize_location` edge cases consulted by agents working on scorer tasks: \\\"Leading `/` stripped only for path+line form\\\", \\\"No-line input (bare path): regex finds no match \u2192 raw.strip() returned unmodified \u2014 do NOT test slash stripping on bare paths\\\" | Risk: MEDIUM | Why: agent memory files are actively read by agents to avoid re-discovering edge cases; if behavior changes, agents reading this file would apply the wrong contract and write tests against stale assumptions\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/references/orchestrator-playbook.md` \u2014 references `reduce.py` as \\\"a working, tested implementation\\\" with the `uv run` invocation; describes the core dedup algorithm without specifying `normalize_location` semantics precisely | Risk: LOW | Why: does not expose `normalize_location` contract in enough detail to become actively misleading; minor update needed only if the CLI changes (new flag for compat gate)\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/references/measuring-success.md` \u2014 brief reference to `reduce.py` as the dedup mechanism | Risk: LOW | Why: no contract details exposed; minimal update needed\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/SKILL.md` \u2014 references `reduce.py` and `test_reduce.py`; describes corroboration on `(group, location)` without specifying normalize_location internals | Risk: LOW | Why: description is at the algorithm level; a compat gate does not change the algorithm description; update needed only if CLI interface changes\\n\\n### Configuration / CI\\n\\n- `pyproject.toml` \u2014 `testpaths` includes `examples/solid-review-ab/tests` (covers `test_scorer.py` and `test_scorer_independent.py`) but does NOT include `plugins/plugin-creator/skills/ensemble-rule-review/scripts/` (where `test_reduce.py` lives); `test_reduce.py` is a manually-invoked standalone test | Risk: MEDIUM | Why: `test_reduce.py` \u2014 the primary test for the changed function \u2014 does not run in automated CI; a regression in `normalize_location` or the new compat gate would not be caught until manually run; recommend adding the scripts directory to testpaths or creating a conftest-forwarding test in `plugins/plugin-creator/tests/`\\n\\n- `.github/workflows/code-quality.yml` and related CI workflows \u2014 no direct reference to `reduce.py`, `test_reduce.py`, or `ensemble-rule-review`; CI coverage for `normalize_location` changes relies entirely on `examples/solid-review-ab/tests` being in testpaths | Risk: LOW | Why: no immediate CI breakage; existing scorer tests provide a partial safety net; the gap is `test_reduce.py` not running automatically\\n\\n### Agent Instructions\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/assets/worker-prompt-skeleton.md` \u2014 instructs workers to run `reduce.py` and defines the output schema; workers emit `location: <path:line>` format; if heading-normalization ships, workers using heading anchors in location fields (e.g. `file.py#section`) would be processed differently without any instruction update | Risk: LOW | Why: current instructions explicitly target `path:line` format; heading anchors in worker output are not a documented use case; no instruction update needed unless heading inputs are explicitly added as a supported location format\\n\\n- `examples/solid-review-ab/multi-low-intelligence-focused-agents/.claude/skills/review-against-solid-principles/SKILL.md` \u2014 instructs Haiku workers: \\\"write location as `corpus/cases/<file>:<line>`\\\"; encodes the `path:line` format as the required output | Risk: LOW | Why: `path:line` instruction is safe regardless of whether heading normalization is gated or not; this instruction would only need updating if the arm explicitly adopted heading-anchor location format\\n\\n### Systems Inventory\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py` | Role: producer | Connection: defines `normalize_location()` and `reduce_findings()` \u2014 the API under change; must gain a backward-compat parameter before heading-normalization behavior can be added | Action: CODE_CHANGE | Risk: HIGH | pattern: 'normalize_location'\\n\\n- `examples/solid-review-ab/runner/scorer.py` | Role: consumer | Connection: imports `normalize_location` and `reduce_findings` from reduce.py via importlib; any unconditional behavior change propagates silently | Action: VERIFY_COMPATIBLE | Risk: HIGH | pattern: '_reduce.normalize_location'\\n\\n- `examples/solid-review-ab/corpus/gold.json` | Role: other-reference | Connection: defines expected `(group, location)` keys in `path:line` format; normalize_location output must produce matching strings or all metrics become zero | Action: VERIFY_COMPATIBLE | Risk: HIGH\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/scripts/test_reduce.py` | Role: test | Connection: directly tests `normalize_location()` and `reduce_findings()`; not in CI testpaths; encodes current behavior only | Action: TEST_UPDATE | Risk: MEDIUM | pattern: 'normalize_location'\\n\\n- `examples/solid-review-ab/tests/test_scorer.py` | Role: test | Connection: tests `normalize_location` via scorer wrapper; 4 contract tests; in CI testpaths | Action: TEST_UPDATE | Risk: MEDIUM | pattern: 'normalize_location'\\n\\n- `examples/solid-review-ab/tests/test_scorer_independent.py` | Role: test | Connection: independently-authored tests for `normalize_location` edge cases; in CI testpaths | Action: TEST_UPDATE | Risk: MEDIUM | pattern: 'normalize_location'\\n\\n- `examples/solid-review-ab/README.md` | Role: documentation | Connection: documents normalize_location contract for arm authors and gold label authors; encodes current behavior only | Action: CONTENT_UPDATE | Risk: MEDIUM\\n\\n- `examples/solid-review-ab/.claude/agent-memory/python-engineering-python-pytest-architect/project_scorer_independence.md` | Role: agent-instruction | Connection: agent memory consulted when writing scorer-adjacent tests; documents normalize_location edge cases | Action: CONTENT_UPDATE | Risk: MEDIUM\\n\\n- `pyproject.toml` | Role: configuration | Connection: testpaths does not include `scripts/` dir where `test_reduce.py` lives; coverage gap for the primary changed function | Action: CONFIG_UPDATE | Risk: MEDIUM\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/assets/worker-prompt-skeleton.md` | Role: agent-instruction | Connection: instructs workers on output format used as input to reduce.py | Action: VERIFY_COMPATIBLE | Risk: LOW\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/references/orchestrator-playbook.md` | Role: documentation | Connection: references reduce.py usage; CLI interface may need update if flag added | Action: VERIFY_COMPATIBLE | Risk: LOW\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/SKILL.md` | Role: documentation | Connection: references reduce.py and test_reduce.py; describes algorithm at high level | Action: VERIFY_COMPATIBLE | Risk: LOW\\n\\n- `plugins/plugin-creator/skills/ensemble-rule-review/scripts/plan_ensemble.py` | Role: other-reference | Connection: architectural pair to reduce.py; no direct normalize_location call | Action: VERIFY_COMPATIBLE | Risk: LOW\\n\\n- `examples/solid-review-ab/multi-low-intelligence-focused-agents/.claude/skills/review-against-solid-principles/SKILL.md` | Role: agent-instruction | Connection: instructs Haiku workers to emit `path:line` format | Action: VERIFY_COMPATIBLE | Risk: LOW\\n\\n### Risk Summary\\n\\n- Overall system risk: HIGH\\n- Highest-risk systems:\\n  - `reduce.py::normalize_location` \u2014 zero-parameter API with no opt-out surface; adding behavior here without a gate is an unconditional breaking change for all consumers\\n  - `scorer.py` \u2014 hard-coupled via importlib; a silent behavior change corrupts P/R/F1 metrics with no runtime error\\n  - `corpus/gold.json` \u2014 handcrafted ground truth in `path:line` format; any asymmetric normalization silently degrades all evaluation metrics to zero without surfacing an exception\\n- Main risk themes:\\n  - compatibility \u2014 no parameter on `normalize_location()` means callers cannot opt out\\n  - behavioral \u2014 normalization key change propagates silently through the entire evaluation pipeline\\n  - verification \u2014 `test_reduce.py` does not run in CI; no tests exist for heading inputs or the gate parameter\\n  - scope \u2014 the change is narrow in code size but touches the corroboration key used across the full evaluation pipeline\\n- Scope expansion:\\n  - `corpus/gold.json` was not identified in the original item description but is directly in scope: its `path:line` format keys are the downstream target of normalize_location's output; any behavior change without gate would require gold label audit\\n\\n### Ecosystem Completeness Checklist\\n\\n- [ ] Every code producer updated or verified compatible (reduce.py must gain backward-compat parameter before heading-normalization behavior lands)\\n- [ ] Every code consumer migrated or verified compatible (scorer.py must explicitly opt in or confirm old-behavior default is preserved)\\n- [ ] Every affected test updated or new coverage added (test_reduce.py, test_scorer.py, test_scorer_independent.py must gain tests for gate semantics; no current test covers heading-anchor inputs)\\n- [ ] Every stale document updated (README.md normalize_location contract section; agent memory edge-case doc)\\n- [ ] Every affected config or CI path updated and validated (pyproject.toml testpaths should include test_reduce.py or a forwarding test in plugins/plugin-creator/tests/)\\n- [ ] Every affected agent or skill instruction updated (verify worker prompt and SKILL.md are compatible with any compat gate CLI change)\\n- [ ] Replacement or migration coverage gaps resolved (no migration scenario; compat gate is additive)\\n- [ ] Hidden transitive dependencies checked (gold.json confirmed in scope; no additional hidden consumers found beyond the 14 files enumerated)\\n</div>\\n\\n## Acceptance Criteria\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\n- [ ] Work matches description\\n- [ ] Plan or implementation complete\\n</div>\\n\\n## Story\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\nAs a **developer relying on this plugin**, I want to **reduce.py's normalize_location changes shipped without the required backward-compat gate** so that **the tool works correctly and reliably**.\\n</div>\\n\\n## Context\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\n- **Source**: Agent task - auto-derived from /code-review max --fix pr session, PR #2626\\n- **Priority**: P1\\n- **Added**: 2026-07-03\\n- **Research questions**: None\\n</div>\\n\\n## Groomed (2026-07-06)\\n\\n### Priority\\n\\n<div><sub>2026-07-06T05:56:00.335188Z</sub>\\n\\n8/10 \u2014 This is a blocking prerequisite for PR #2626. The zero-parameter API is a single point of failure for the entire evaluation pipeline: `scorer.py` imports `normalize_location` via importlib with no abstraction layer, and any unconditional behavior change silently corrupts `(group, location)` keys used for P/R/F1 computation against `corpus/gold.json`. Silent metric corruption (scores drop to zero, no exception raised) is the failure mode. The CI gap for `test_reduce.py` means the regression would not be caught automatically. The fix scope is narrow but must land before PR #2626 can merge safely.\\n</div>\\n\\n### Impact\\n\\n<div><sub>2026-07-06T05:56:12.910263Z</sub>\\n\\n- Blocks: PR #2626 from landing safely; any heading-normalization behavior addition to `normalize_location()` cannot merge without corrupting existing callers\\n- Bottleneck: `normalize_location()` is the sole normalization point for all corroboration keys across the evaluation pipeline; zero-parameter API means every caller inherits every future change unconditionally\\n- Consumer at risk: `scorer.py` imports via `importlib` with no abstraction layer; a behavior change propagates directly into `(group, location)` key construction for P/R/F1 matching against `corpus/gold.json`; the failure mode is silent \u2014 all metrics drop to zero with no exception raised, no log entry, and no warning until the path-mismatch heuristic fires (which only triggers when ALL keys miss, not partial)\\n- CI gap amplifier: `test_reduce.py` \u2014 the only direct test of `normalize_location()` \u2014 does not run in automated CI; a regression introduced by a future change would not be caught until manually invoked\\n</div>\\n\\n### Benefits\\n\\n<div><sub>2026-07-06T05:56:32.549429Z</sub>\\n\\n- PR #2626 heading-normalization changes can be merged safely once the gate exists; callers that have not opted in are unaffected\\n- `scorer.py` continues to produce correct P/R/F1 metrics against `corpus/gold.json` without code changes, because the old-behavior default is preserved\\n- `test_reduce.py` running in CI catches future regressions to `normalize_location()` automatically at PR time instead of requiring manual invocation\\n- Gate semantics verified by tests give future maintainers a machine-checkable contract that existing callers are not silently broken by behavior additions\\n- Removes the single-point-of-failure property of the zero-parameter API: future behavior changes can be staged incrementally, with callers opting in on their own schedule\\n</div>\\n\\n### Expected Behavior\\n\\n<div><sub>2026-07-06T05:56:45.214128Z</sub>\\n\\nWhen this item is resolved:\\n\\n- `normalize_location()` accepts a parameter that allows callers to control whether new behavior applies; the default preserves the current behavior (strip absolute prefix, normalize to `path:line`, preserve anchor patterns) so every existing caller continues to work without code changes.\\n- `scorer.py`, called without modification, produces the same `(group, location)` keys as before and all P/R/F1 metrics against `corpus/gold.json` remain valid.\\n- Running `uv run pytest` without manual path arguments executes `test_reduce.py` as part of the automated suite.\\n- At least one test verifies that calling `normalize_location()` with the backward-compat default returns the same output as the current implementation for a standard `path:line` input.\\n- At least one test verifies that the gate parameter allows access to the new behavior when a caller explicitly opts in.\\n- No currently-passing test becomes red as a result of the gate addition.\\n</div>\\n\\n### Acceptance Criteria\\n\\n<div><sub>2026-07-06T05:56:57.831343Z</sub>\\n\\n1. `def normalize_location(` in `reduce.py` has a parameter providing a backward-compat opt-out; the default value preserves the current behavior for all callers that pass no extra argument.\\n2. All four tests in `examples/solid-review-ab/tests/test_scorer.py` and all three tests in `examples/solid-review-ab/tests/test_scorer_independent.py` pass without any changes to `scorer.py` \u2014 confirming existing consumers inherit the old-behavior default.\\n3. `uv run pytest --collect-only` includes `test_reduce.py` in its output \u2014 either because `pyproject.toml testpaths` now includes the `scripts/` directory or a forwarding conftest in `plugins/plugin-creator/tests/` pulls it in.\\n4. `uv run pytest plugins/plugin-creator/skills/ensemble-rule-review/scripts/test_reduce.py` (or its CI-equivalent path) passes, including at least one test that exercises the gate parameter directly: one assertion for old-behavior default, one assertion for new-behavior opt-in.\\n5. No test in the full `uv run pytest` run that was green before this change becomes red after it \u2014 verified by running the full suite before and after.\\n</div>\\n\\n### Resources\\n\\n<div><sub>2026-07-06T05:57:48.658892Z</sub>\\n\\n| Type | Item |\\n|------|------|\\n| Skill | `/plugin-creator:ensemble-rule-review` \u2014 owns `reduce.py` and the corroboration pipeline |\\n| Prior work | `plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py` \u2014 function under change |\\n| Prior work | `examples/solid-review-ab/runner/scorer.py` \u2014 high-risk importlib consumer |\\n| Prior work | `examples/solid-review-ab/corpus/gold.json` \u2014 ground truth keys must remain compatible |\\n| Test | `plugins/plugin-creator/skills/ensemble-rule-review/scripts/test_reduce.py` \u2014 direct unit tests (not in CI) |\\n| Test | `examples/solid-review-ab/tests/test_scorer.py` \u2014 4 contract tests (in CI) |\\n| Test | `examples/solid-review-ab/tests/test_scorer_independent.py` \u2014 3 independent edge-case tests (in CI) |\\n| Docs | `examples/solid-review-ab/README.md` \u2014 normalize_location behavioral contract for arm authors |\\n| Docs | `examples/solid-review-ab/.claude/agent-memory/python-engineering-python-pytest-architect/project_scorer_independence.md` \u2014 agent edge-case memory |\\n| Docs | `plugins/plugin-creator/skills/ensemble-rule-review/references/orchestrator-playbook.md` |\\n| Backlog | PR #2626 \u2014 the pending change that requires the gate to exist before it can safely land |\\n</div>\\n\\n### Dependencies\\n\\n<div><sub>2026-07-06T05:58:01.183304Z</sub>\\n\\n- Depends on: None \u2014 this item creates the prerequisite that other work depends on\\n- Blocks: PR #2626 (heading-normalization changes to `normalize_location()` cannot be merged safely until the backward-compat gate exists and is tested in CI)\\n</div>\\n\\n### Effort\\n\\n<div><sub>2026-07-06T05:58:13.982186Z</sub>\\n\\nSmall \u2014 The code change is narrow: one function signature, one default, one internal call site in `reduce_findings`. The broader effort spans CI configuration (one line in `pyproject.toml` or a forwarding conftest), new gate-semantics tests in `test_reduce.py`, verification that `scorer.py` remains green without changes, and confirmation that `corpus/gold.json` keys are unaffected. End-to-end validation requires running the full pytest suite before and after. Risk is concentrated in the silent-failure mode of `scorer.py` metrics, so the verification step cannot be skipped.\\n</div>\\n\\n### Files\\n\\n<div><sub>2026-07-06T05:57:36.023774Z</sub>\\n\\n| File | Role | Risk | Action |\\n|------|------|------|--------|\\n| `plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py` | Producer \u2014 defines `normalize_location()` and `reduce_findings()` | HIGH | CODE_CHANGE \u2014 must gain backward-compat parameter before heading-normalization behavior can land |\\n| `examples/solid-review-ab/runner/scorer.py` | Consumer \u2014 imports via `importlib`; builds `(group, location)` keys for P/R/F1 | HIGH | VERIFY_COMPATIBLE \u2014 must remain correct with old-behavior default |\\n| `examples/solid-review-ab/corpus/gold.json` | Reference \u2014 handcrafted ground truth in `path:line` format; keys must match normalize_location output exactly | HIGH | VERIFY_COMPATIBLE \u2014 any asymmetric normalization silently zeroes all metrics |\\n| `plugins/plugin-creator/skills/ensemble-rule-review/scripts/test_reduce.py` | Test \u2014 directly tests `normalize_location()`; NOT in CI testpaths | MEDIUM | TEST_UPDATE + CONFIG_UPDATE \u2014 add to testpaths; extend with gate-semantics tests |\\n| `examples/solid-review-ab/tests/test_scorer.py` | Test \u2014 4 contract tests via scorer wrapper; in CI testpaths | MEDIUM | TEST_UPDATE \u2014 add tests for gate parameter if scorer wrapper is updated |\\n| `examples/solid-review-ab/tests/test_scorer_independent.py` | Test \u2014 3 independently-authored edge-case tests; in CI testpaths | MEDIUM | TEST_UPDATE \u2014 add tests for gate parameter if scorer wrapper is updated |\\n| `pyproject.toml` | Config \u2014 `testpaths` excludes `scripts/` directory | MEDIUM | CONFIG_UPDATE \u2014 add path so `test_reduce.py` runs in CI |\\n| `examples/solid-review-ab/README.md` | Docs \u2014 normalize_location contract specification for arm authors (line 84) | MEDIUM | CONTENT_UPDATE \u2014 update to reflect gate parameter once added |\\n| `examples/solid-review-ab/.claude/agent-memory/python-engineering-python-pytest-architect/project_scorer_independence.md` | Agent memory \u2014 edge-case notes read by agents writing scorer-adjacent tests | MEDIUM | CONTENT_UPDATE \u2014 update if behavior changes; stale notes cause agents to write wrong tests |\\n| `plugins/plugin-creator/skills/ensemble-rule-review/references/orchestrator-playbook.md` | Docs \u2014 references reduce.py usage and CLI invocation | LOW | VERIFY_COMPATIBLE \u2014 update CLI section only if gate adds a flag |\\n| `plugins/plugin-creator/skills/ensemble-rule-review/SKILL.md` | Docs \u2014 algorithm-level description; no normalize_location internals | LOW | VERIFY_COMPATIBLE |\\n| `plugins/plugin-creator/skills/ensemble-rule-review/assets/worker-prompt-skeleton.md` | Agent instruction \u2014 workers emit `path:line` format; no heading anchors in scope | LOW | VERIFY_COMPATIBLE |\\n| `plugins/plugin-creator/skills/ensemble-rule-review/scripts/plan_ensemble.py` | Other \u2014 architectural pair to reduce.py; no direct normalize_location call | LOW | VERIFY_COMPATIBLE |\\n</div>\\n\\n### Issue Classification\\n\\n<div><sub>2026-07-06T05:47:21.040441Z</sub>\\n\\n**Type**: missing-guardrail\\n\\n**Rationale**: The decision-tree path is Q4. The system allows a bad outcome that a quality gate should prevent. normalize_location() has no backward-compat parameters \u2014 any behavior changes apply unconditionally to all callers (verified: function has no opt-out parameters). Downstream consumers like scorer.py have tight coupling to this utility with silent failure modes: changes corrupt metrics with no exception raised. Additionally, test_reduce.py is excluded from CI (not in pyproject.toml testpaths), eliminating contract validation. The design permits unconditional changes without mechanism to opt out or validate impact.\\n\\n**Analysis Method**: N/A (missing-guardrail does not require root-cause analysis)\\n\\n**Scenario Target**: System allows unconditional changes to normalize_location() with no backward-compat gate; consumers silently break with corrupted metrics; contract validated only outside CI \u2192 Changes to normalize_location() gated behind backward-compat flag; consumers opt old or new behavior; test_reduce.py runs in CI to catch contract violations\\n</div>\\n\\n### Reproducibility\\n\\n<div><sub>2026-07-06T05:55:47.848457Z</sub>\\n\\nThe missing guardrail is observable without triggering a behavior change \u2014 the gap is in the API surface and CI configuration, not in current runtime behavior.\\n\\n1. Inspect the function signature: `grep -n \\\"def normalize_location\\\" plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py` \u2014 output shows `def normalize_location(raw: str) -> str:` with zero parameters; no opt-out surface exists for any caller.\\n2. Confirm no heading-normalization behavior exists in current main: `grep -n \\\"heading\\\\|anchor\\\\|#\\\" plugins/plugin-creator/skills/ensemble-rule-review/scripts/reduce.py` \u2014 no matches; the current regex `LOCATION_LINE_RE = re.compile(r\\\"(\\\\S+):(\\\\d+)\\\")` preserves anchor patterns unchanged.\\n3. Confirm CI gap: `grep -A 20 \\\"testpaths\\\" pyproject.toml` \u2014 `plugins/plugin-creator/skills/ensemble-rule-review/scripts/` is absent from testpaths; `examples/solid-review-ab/tests` is present.\\n4. Confirm test_reduce.py is excluded from automated CI: `uv run pytest --collect-only 2>&1 | grep test_reduce` \u2014 no output; the file does not appear in the collected suite.\\n5. Confirm no existing test covers heading-anchor input or a gate parameter: `grep -rn \\\"heading\\\\|anchor\\\\|#.*py\\\\|gate\\\\|compat\\\" plugins/plugin-creator/skills/ensemble-rule-review/scripts/test_reduce.py examples/solid-review-ab/tests/` \u2014 no matches.\\n</div>\\n\",\"digest\":\"c1308975286ea2bcc685a97ec8d9e91197f7f68fafa51857f150578f9e607663\",\"comment_id\":\"IC_kwDOQGimCs8AAAABPg1P-g\"}"}