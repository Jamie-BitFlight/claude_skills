{"version":1,"reference":{"kind":"artifact_content","namespace":"#3195","artifact_type":"_dh-work-item-head-v1","name":"head"},"owner_reference":"#3195","content":"{\"version\":1,\"issue_reference\":\"#3195\",\"parent_revision\":\"da498ff6600d820b5552958af94302301c2a3668717ec3187d37e5648e978f75\",\"root_revision\":\"da498ff6600d820b5552958af94302301c2a3668717ec3187d37e5648e978f75\",\"body\":\"<!-- backlog-metadata:\\npriority: P2\\ntype: Refactor\\nstatus: open\\nadded: 2026-08-23\\n-->\\n\\n## Description\\n\\n## Description\\n\\n`AGENTS.md`'s Python conventions state: \\\"Structured data \u2192 Pydantic, not dataclass/TypedDict: this repo's ingestion and output objects are standardizing on Pydantic `BaseModel`... When adding or touching a structured data shape (CLI output, MCP tool payloads, parsed file records), use Pydantic `BaseModel` by default.\\\"\\n\\nSeven classes in `plugins/development-harness/backlog_core/models.py` are `TypedDict` rather than `BaseModel`, and each is used in an MCP tool response or API output shape rather than a stdlib-only context:\\n\\n- `BranchInfo` (line 645)\\n- `MergeResult` (line 661)\\n- `SamTasksResult` (line 1343) \u2014 return type for `get_sam_tasks`\\n- `SectionEntryDict` (line 1369) \u2014 used in `ViewItemResult` API output\\n- `SectionEntryMetadata` (line 1387) \u2014 used in `ViewItemResult.sections`\\n- `GroomedSectionMetadata` (line 1409) \u2014 used in `ViewItemResult.sections`\\n- `SectionMeta` (line 1461) \u2014 used in `ViewItemResultCompact.sections_metadata`\\n\\nThe same module already imports `pydantic` and uses `BaseModel` extensively (`ViewItemResult`, `ViewItemResultCompact`), so no stdlib-only constraint applies \u2014 the exception AGENTS.md carries for genuinely dependency-constrained modules does not cover this file.\\n\\n## Verification status\\n\\nFound by a `plugin-creator:ensemble-rule-review` worker checking PR #3147's touched files against this repo's own conventions. Confirmed these seven classes exist at the cited lines and are `TypedDict`, and confirmed the file imports `pydantic`. Not independently confirmed: whether converting each is a pure rename or whether any consumer pattern-matches on `dict`-shaped access (`result[\\\"key\\\"]`) in a way that would need updating alongside the type change \u2014 that would need checking each call site before implementation.\\n\\n## Not related to PR #3147\\n\\nNone of these seven classes were touched by #3147's diff (confirmed: #3147's only change to this file is adding one `StrEnum` member). This is pre-existing code, filed as a separate item so it does not block or get confused with that PR's review.\\n\\n## User-provided context\\n\\nConverting a `TypedDict` used as a dict-shaped return value to `BaseModel` can change call-site ergonomics (`result[\\\"key\\\"]` vs `result.key`, or requiring `.model_dump()` at serialization boundaries) \u2014 worth scoping per-class during grooming rather than assuming a uniform mechanical conversion.\\n\\n## Context\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\n- **Source**: Ensemble-rule-review pass on PR #3147, 2026-08-23\\n- **Priority**: P2\\n- **Added**: 2026-08-23\\n- **Research questions**: None\\n</div>\\n\\n## Verification Status\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\nFound by a `plugin-creator:ensemble-rule-review` worker checking PR #3147's touched files against this repo's own conventions. Confirmed these seven classes exist at the cited lines and are `TypedDict`, and confirmed the file imports `pydantic`. Not independently confirmed: whether converting each is a pure rename or whether any consumer pattern-matches on `dict`-shaped access (`result[\\\"key\\\"]`) in a way that would need updating alongside the type change \u2014 that would need checking each call site before implementation.\\n</div>\\n\\n## User Provided Context\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\nConverting a `TypedDict` used as a dict-shaped return value to `BaseModel` can change call-site ergonomics (`result[\\\"key\\\"]` vs `result.key`, or requiring `.model_dump()` at serialization boundaries) \u2014 worth scoping per-class during grooming rather than assuming a uniform mechanical conversion.\\n</div>\\n\\n## Not Related To Pr 3147\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\nNone of these seven classes were touched by #3147's diff (confirmed: #3147's only change to this file is adding one `StrEnum` member). This is pre-existing code, filed as a separate item so it does not block or get confused with that PR's review.\\n</div>\\n\",\"digest\":\"83fd3c619449d4dc28f090513f01d35e6e80f0adf27f4e52c6af7f3acd4cf20d\",\"comment_id\":\"IC_kwDOQGimCs8AAAABQTRx9w\"}"}