{"version":1,"reference":{"kind":"artifact_content","namespace":"#3874","artifact_type":"_dh-work-item-head-v1","name":"head"},"owner_reference":"#3874","content":"{\"version\":1,\"issue_reference\":\"#3874\",\"parent_revision\":\"7bb9e9ef02da2fcbcefbd822c4b586f8952234748f732bdf865e888aab557245\",\"root_revision\":\"7bb9e9ef02da2fcbcefbd822c4b586f8952234748f732bdf865e888aab557245\",\"body\":\"<!-- backlog-metadata:\\npriority: P1\\ntype: Bug\\nstatus: open\\nadded: 2026-09-24\\n-->\\n\\n## Description\\n\\n## Desired outcome\\n\\n`artifact_read` without `artifact_id` must return the established artifact-list response so the caller can select the correct artifact. Omitted identity always lists, including when exactly one artifact exists. Zero matches returns an empty list with count zero. Content is returned only for an explicit artifact identity. The operation, all callers, tests, and documentation must move together so no consumer retains newest-wins assumptions.\\n\\n## Observed gap and evidence\\n\\nCurrent `artifact_read` behavior selects the newest artifact when the identity is omitted, and current artifact-registry documentation describes that behavior. The targeted issue search found no existing owner for this migration.\\n\\n## Impact\\n\\nA caller can silently read the wrong state artifact and act on it. The behavior also forces another turn after an identity mistake instead of returning the available references.\\n\\n## Scope boundaries\\n\\nThis Work Brief owns omitted-versus-explicit identity behavior and the compatibility migration. It does not change artifact contents, define a default artifact, or add unrelated pagination behavior.\\n\\n## Readiness and acceptance conditions\\n\\nFor zero, one, and multiple matching artifacts, omitted identity returns list metadata with the correct count. Explicit identity returns only that artifact's content. No caller, test, or documentation path relies on newest-wins after the migration.\\n\\n## Impact\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\nA caller can silently read the wrong state artifact and act on it. The behavior also forces another turn after an identity mistake instead of returning the available references.\\n</div>\\n\\n## Context\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\n- **Source**: User-confirmed Work Brief grilling session; current artifact_read behavior verified 2026-09-24\\n- **Priority**: P1\\n- **Added**: 2026-09-24\\n- **Research questions**: None\\n</div>\\n\\n## Desired Outcome\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\n`artifact_read` without `artifact_id` must return the established artifact-list response so the caller can select the correct artifact. Omitted identity always lists, including when exactly one artifact exists. Zero matches returns an empty list with count zero. Content is returned only for an explicit artifact identity. The operation, all callers, tests, and documentation must move together so no consumer retains newest-wins assumptions.\\n</div>\\n\\n## Readiness And Acceptance Conditions\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\nFor zero, one, and multiple matching artifacts, omitted identity returns list metadata with the correct count. Explicit identity returns only that artifact's content. No caller, test, or documentation path relies on newest-wins after the migration.\\n</div>\\n\\n## Scope Boundaries\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\nThis Work Brief owns omitted-versus-explicit identity behavior and the compatibility migration. It does not change artifact contents, define a default artifact, or add unrelated pagination behavior.\\n</div>\\n\\n## Observed Gap And Evidence\\n\\n<div><sub>0000-00-00T00:00:00Z</sub>\\n\\nCurrent `artifact_read` behavior selects the newest artifact when the identity is omitted, and current artifact-registry documentation describes that behavior. The targeted issue search found no existing owner for this migration.\\n</div>\\n\",\"digest\":\"d585b7895263427c65d9b7a126915ac35defe6b5b5c089ca53846ecac9068eaa\",\"comment_id\":\"IC_kwDOQGimCs8AAAABWnr2nA\"}"}